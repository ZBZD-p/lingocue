fastapi
uvicorn
pydantic

# mcp_server.py 用它把字幕/播放查询暴露给 Claude Code CLI。
# 只用 DeepSeek 引擎的话用不到，但装上不影响。
mcp>=2.0.0

# 可选：YouTube 自动字幕完全没标点时，youtube.py 用本地 ct-punc 模型
# 校对断句（见 polish_auto_captions_in_background）。会带上 torch，体积不小。
# Windows 上建议先手动装好对应平台的 torch + torchaudio（CPU 版：
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu），
# 有 GPU 就装匹配的 CUDA 版，再装 funasr。torchaudio 单独列出来是因为
# funasr 实测不会自动把它拉下来，但 import funasr 时又硬需要，缺了会直接报
# ModuleNotFoundError。首次真正用到 ct-punc 时还会从 ModelScope 现下模型
# （约 290M 参数），要联得上网。
funasr
