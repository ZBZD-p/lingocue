fastapi
uvicorn
pydantic

# YouTube 字幕就靠这一个包（youtube.py 的 _caption_tracks）。之前用的是
# yt-dlp，但那是个"取视频"的工具，为此要过 YouTube 完整的播放器 API：JS 挑战
# （得再装 Deno）、已登录请求还要 PO token（它生成不了，结果是自动字幕直接
# 消失）。我们从头到尾只读字幕、一帧视频都不下，那套关卡全是白付的代价。
# 直接打字幕接口就没这些事，顺带省掉 Deno、远程解题脚本和整套 cookie 处理。
youtube-transcript-api
requests

# mcp_server.py 用它把字幕/播放查询暴露给 Claude Code CLI。
# 只用 DeepSeek 引擎的话用不到，但装上不影响。
mcp>=2.0.0

# ---- 以下是可选的，故意注释掉，不要取消注释 ----------------------------
# YouTube 自动字幕完全没标点时，youtube.py 用本地 ct-punc 模型校对断句
# （见 polish_auto_captions_in_background）。
#
# 这一段以前 funasr 是没注释的，等于每个人 pip install -r 都会装上它——而它
# 会拖来 transformers / numba / scikit-learn / librosa / scipy 整棵树，实测让
# 一份干净环境从 62MB 涨到 748MB，还没算 torch。setup.ps1 的 -WithPunctuation
# 开关和 README 里「按需要的」那张表都说它是可选的，只有这个文件不是，所以
# 是这里错了。
#
# 要用的话装法（顺序有讲究）：
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
#   pip install funasr
# torchaudio 要单独列是因为 funasr 实测不会自动把它拉下来，但 import funasr
# 时又硬需要，缺了直接 ModuleNotFoundError。有 GPU 就换成匹配的 CUDA 版。
# 首次真正用到 ct-punc 时还会从 ModelScope 现下模型（约 290M 参数），要联网。
# 启动器的「环境」页里点「标点优化 → 安装」就是按上面这两条跑的。
#
# funasr
